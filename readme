for Question 2
Constructs the adjacency list and computes the in-degree for each module.

Initializes a queue with modules having zero in-degree (that is, no dependencies).

Performs a BFS traversal  while counting visited nodes.

If the number of visited nodes is less than n, a cycle exists.
